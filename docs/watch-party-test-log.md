# Watch Party implementation test log

## Full test pass 1 — 2026-07-13

- `npm --prefix socket-server test`: PASS — 3/3 tests.
- `npm run build`: PASS — production compile, type validation, static generation and route generation completed.
- `tsc --noEmit`: FAIL — stale generated `.next/types/app/api/proxy/route.ts` references a deleted `app/api/proxy/route.js`. This is generated-cache residue; the production build itself regenerated valid route types successfully.
- Non-blocking warnings: outdated Browserslist database, missing `metadataBase`, one ambiguous Tailwind arbitrary easing class.

## Defects queued after pass 1

1. P1 — standalone `tsc --noEmit` is affected by stale `.next` generated types.
2. P1 — playback and episode client commands need acknowledgement/error telemetry instead of fire-and-forget emits.
3. P2 — create-room UI should state clearly when no HLS source is available.

No defect was fixed before this log and queue were created.

## Regression pass 2 — 2026-07-13

- Socket unit tests: PASS — 3/3.
- `tsc --noEmit`: PASS after `.next` route types were regenerated by the production build.
- `npm run build`: PASS — 40/40 static pages, including `/history`.
- `git diff --check`: PASS (line-ending notices only).
- Code review found P1 race: `saveProgress` relied on a React state-updater side effect before deciding whether to write Firebase. Queued for immediate correction, then final regression.

## Final regression pass 3 — 2026-07-13

- P1 history race corrected by constructing the V2 movie record synchronously before local and Firebase writes.
- Socket unit tests: PASS — 3/3.
- `tsc --noEmit`: PASS.
- `npm run build`: PASS — production bundle and all 40 static pages generated.
- `git diff --check`: PASS; only Windows LF/CRLF conversion notices remain.
- Remaining build warnings are pre-existing/non-blocking: Tailwind ambiguous easing class and missing Next.js `metadataBase`.

## HLS incident — room 8CKH54

- Browser symptom: `HTMLMediaElement` error, duration `0:00`.
- Direct CDN navigation: `net::ERR_BLOCKED_BY_CLIENT` for `v7.kkphimplayer7.com`.
- Socket server probe: upstream is healthy (`200`, `application/vnd.apple.mpegurl`).
- Root cause: client/browser blocks the CDN hostname; WebSocket synchronization is not involved.
- Fix: player now requests HLS through `/api/media/proxy`; server allowlists KKPhim CDN hosts and rewrites master playlists, child playlists, segment and key URIs.
- Live isolated verification on port 4011: `200`, correct HLS MIME type, CORS for `localhost:3000`, and rewritten child-manifest URL.
- Syntax check, socket unit tests (3/3), and TypeScript: PASS.
- A concurrent `next dev` process made `next build` contend for `.next` and fail page collection for unrelated `/ai-recommender`; run production build only after stopping dev.
